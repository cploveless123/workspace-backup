# 2026-04-09

## Wallet Monitor
- 00:43 UTC: suqh5sHtr... reduced 2.32→2.19 SOL (profit taking), CyaE1Vxv... reduced 356.51→349.32 SOL (~7.2 SOL outflow). No new tokens detected.

## CRITICAL BUG FIXES (00:59 UTC)
### STOP LOSS BUG - CRITICAL
- position_monitor.py had stop loss code REMOVED during TP1 rewrite
- Caused JRIP to lose -74.9% instead of -20% stop loss
- Fixed: gains_pct defined at top of loop, stop loss restored as FIRST check before TP logic
- Code verification: grep -c "gains_pct = " position_monitor.py should return 1
- Position monitor completely rewritten to fix scoping issues
- All scanners restarted with clean imports

### JRIP loss analysis:
- Bought: 00:30 UTC @ $10,505
- Peaked: ~$18K (+71%) — well past +50% TP1 threshold
- Dumped to: $2,639 (-74.9%) → stop loss finally fired at 00:37
- Cost: -0.0374 SOL instead of -0.0101 SOL (-20% stop)

### Current state (01:05 UTC):
- Balance: ~0.9200 SOL | 2W/10L (17% WR after FAUCET TP1 recovery)
- Open: 0 positions (all stopped out or TP'd)
- All 4 systems running: auto_scanner, gmgn_buyer, position_monitor, alert_sender

## Whale 17 Added
- Whale: DiArbWyMSbCe2AGQ2BZ337Lctc8DcgcwYAFKgr6ovGw1 (Jrip)
- Stats: 52.2% WR, +$12,393 realized PnL, 2.2h avg hold
- Style: Ultra-selective fast scalp ($2,479 per token avg)
- Combined 17-whale: Avg WR = 55.7%, Avg hold = 110h

## Backups
- Dex-trading-bot: e528aec
- workspace-backup: 975da49
## CRITICAL BUG: closed_at vs fully_exited (02:XX UTC)
### The Bug
position_monitor.py skipped tokens with `closed_at` set, but `closed_at` is set for BOTH:
- Full exits (stop loss, rug, manual sell)
- TP1 partial exits (partial_exit = True, position still open)

This meant partial positions (LAPD, LOY-002 x2, Cupsey) were NEVER checked by the monitor after TP1 fired. Their trailing stops and stop losses were never triggered.

### Root Cause
The skip logic was: `if t.get('closed_at'): continue`
But TP1 partial exits ALSO set `closed_at` = timestamp of TP1 sale.

### The Fix
Introduced `fully_exited` flag:
- Set `fully_exited = True` on: stop loss, TP2, TP3, trailing stop (full exits)
- NOT set on: TP1 (partial exit - position still open)
- Skip logic: `if t.get('fully_exited'): continue`
- Partial exits (tp1_sold, not trailing_stopped) are still checked for TP2/TP3/stop

### Lesson
NEVER use `closed_at` alone to determine if a position is fully closed. Use a dedicated flag.
When setting `closed_at` for partial exits (TP1), ensure position is still being monitored.
The `status='open_partial'` with `tp1_sold=True` means position still needs monitoring.
Only `trailing_stopped=True` or explicit `fully_exited=True` means fully done.

### Files Fixed
- position_monitor.py: skip logic, fully_exited flag on full exits
- reports/current_trading_report.py, last5_trades.py, wilson_progress.py: use fully_exited check

## SIM RESET at 03:50 UTC
### New Exit Strategy (Chris's latest):
- TP1: +50% minimum → 15% trailing from peak → sell 50%
- TP2: +200% → sell 25%
- TP3: +500% → sell 25%
- Trailing: 30% from peak on remaining
- Stop: -20% (net: -20.5% after 2.5% tax/slippage)
- SIM reset to 1.0 SOL, fresh trades file, peak cache cleared
- Previous session: 5W/21L, ended at 0.3232 SOL (before reset)

## NEW RULE: NEVER RE-ENTER (04:48 UTC)
### Rule: Never re-enter a position we've already sold. Ever.
### Implementation:
- auto_scanner.py: Replaced time-limited re-entry logic with hard block on ANY fully exited token
- gmgn_buyer.py: Added blacklist check in should_buy_from_signal
- Both scanners check: if token_address matches a fully_exited or tp1_sold trade, block immediately
- No exceptions, no time limit, no momentum bypass
