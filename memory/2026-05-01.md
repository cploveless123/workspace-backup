# 2026-05-01 - Memory Log
## 01:24 UTC
- Wallet monitor run: suqh5sHt dropped from 3.71 → 2.41 SOL (-1.3 SOL). CyaE1V whale stable at ~842.9 SOL. No alerts sent.
# 2026-05-01 - Memory Log

## 01:24 UTC
- Wallet monitor run: suqh5sHt dropped from 3.71 → 2.41 SOL (-1.3 SOL). CyaE1V whale stable at ~842.9 SOL. No alerts sent.

## 17:04 UTC - CRITICAL BUG FIXES

### Bug 1: ATH price contaminating peak tracking (FIXED)
- **File:** `position_monitor.py` line 332
- **Problem:** `true_peak = max(current_price, ath_price, peak_price)` — GMGN's all-time high (`ath_price`) was overriding trade-local peak tracking
- **PokéCoin example:** ATH = $39,856, TP2 peak = $20,401 → trail threshold wrongly set to $25,906 (ATH×0.65) instead of $13,260 (TP2×0.65) → exit triggered at $20,535 mcap when it shouldn't have
- **Fix:** `true_peak = max(current_price, peak_price)` — removed `ath_price` from the max()
- **Impact:** TP2 trail stops were 2x too wide because ATH was artificially inflating the peak

### Bug 2: `entry_mcap` from DexScreener instead of GMGN (PENDING FIX)
- **Problem:** Scanner stores `entry_mcap` from DexScreener (e.g., $7,086 for ARI) but `entry_price` from GMGN (e.g., $6,030)
- **Result:** Alert formula `current_mcap = current_price × entry_mcap / entry_price` uses mismatched supplies → wrong mcap
- **Example:** ARI alert showed "Current: $7,956" but real GMGN mcap is $11,528
- **Fix needed:** `entry_mcap` should be `GMGN_price × GMGN_circulating_supply` (calculated from GMGN fields, not DexScreener)
- **Scanner location:** where `entry_mcap` is set from DexScreener data
- **Impact:** All position monitor alerts show wrong mcap figures

### Bug 3: Alert gain % uses `ath_price` in TRUE_PEAK (PENDING FIX)
- **Problem:** `TRUE_PEAK` (line ~307) uses `ath_price` in max() → alerts show inflated peak mcap
- **PokéCoin example:** Alert showed Peak: $21,653 → derived from ATH=$39,856 (wrong), should be TP2 peak=$20,401 → Peak: $20,401

### Positions Affected
- **PokéCoin (5wqEUkAyv2PJq8GfDSeca8ovvHYQRvfDA721Qdygpump):** TP2 trail stop exit was WRONG — exited at $20,536 when threshold was $13,260. Position closed in error.
- **ARI (8zaXZ5rRAveRD3MNA5jfp7AjKNCJe7tek9bK1mRppump):** Open, entry $7,086 (DexScreener mcap), shows "Current: $7,956" but real GMGN mcap is $11,528

### Scanner Running
- gmgn_scanner.py v7.7 with PUMP_CHG1_THRESHOLD=5% restored
- position_monitor.py with ATH fix applied
- Backup pushed to both repos at 17:04 UTC