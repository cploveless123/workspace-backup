# Memory - April 30, 2026

## GMGN API Key (Chris's)
- Location: `~/.gmgn-cli/config.json`
- Key: `gmgn_316679140df01f0712907201654ee2a6`
- GMGN CLI binary: `/usr/bin/gmgn-cli`

## Scanner Fix - Pump Path BUY (April 29-30)
Major fix session resolved 0 buys issue. Root cause: GMGN returns `market_cap: 0` for pump.fun tokens (they use `migration_market_cap` field instead).

### Fixes Applied:
1. **Every mcap fetch** now falls back to `migration_market_cap` when GMGN returns 0
2. **PUMP_VERIFY** uses stored discovery-time mcap as final fallback  
3. **verify_buy_conditions** uses stored mcap when fresh is 0
4. **buy_token** saves `migration_market_cap` as `entry_mcap`
5. **PUMP_WAIT_1 expiry**: uses stored chg1 if GMGN returns 0 (GMGN `price_change_percent1m` = 0 for tokens past 1m window)
6. **PUMP_WAIT_2 expiry**: reuses fresh_data from PUMP_WAIT_1, no new API call

### Result: First pump path BUY
- Token: REPARATIONS
- State machine: `PUMP_WAIT_1 → PUMP_WAIT_2 → PUMP_VERIFY → BUY_PUMP` ✅
- This confirms the full pump path is working

### Stored Fallback Logic:
- GMGN returns 0 for chg1/chg5 after tokens rotate out of their 1-minute change window
- Scanner now preserves discovery-time stored values and uses them when fresh is 0
- Pump path requires: chg1>10%, h1>100%, chg5>10%, age≥120s

## System Status (00:41 UTC):
- Balance: -5.19 SOL | Record: 368W/507L (42% WR)
- Scanner PID: 1424568 | Monitor PID: 1423428
- Open positions: REPARATIONS (pump path ✅), KIRKSLOP (STOP_LOSS at -65%)
- Git backup: workspace (c760342), Dex-trading-bot (ebd8484)

## Balance Issue:
Chris noticed balance showing negative (-5.19 SOL). This is the SIMULATED balance. The real wallet has actual SOL. Scanner uses `sim_wallet.json` for simulation — balance went negative because many trades are closed as losses (screenshot shows closed PnL).

Chris's API key is stored and working. Scanner is running and making buys. No new code changes needed.

## Scanner Log: 9 lines
Normal behavior — scanner runs 1 source per 30s cycle. Each cycle logs ~1-2 lines. Log is not stuck, it's just doing its job quietly.