Scanner was restarted at 04:30 UTC. GMGN APIs worked initially (04:30:02 got 50 tokens) but started failing with 403 shortly after.

The only code changes I made were:
1. KOL path filter changes (chg1 >= 20%, mcap max $15K)
2. Exception handling changes (except: pass → except Exception as e)

The 403 errors started AFTER the scanner restart, not immediately after my code changes. This suggests either:
- Rate limiting from GMGN (too many requests during testing)
- API key issue
- Network issue (IPv6)

Scanner is running with DexScreener fallback. Need to fix GMGN 403s to restore full functionality.